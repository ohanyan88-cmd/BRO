{
  "version": 1,
  "invariants": [
    {"id":"INV-AUTH-001","statement":"Capability never grants authority","enforcement":["tests/test_skills.py","tests/test_immune.py"]},
    {"id":"INV-APPROVAL-001","statement":"Approval canonical task/action bindings cannot be rewritten during consumption","enforcement":["tests/test_approval_recovery_hygiene.py"]},
    {"id":"INV-APPROVAL-002","statement":"Kernel approval resume preserves the same Task, Step and FEET route and a consumed Approval cannot replay","enforcement":["tests/test_kernel_approval.py"]},
    {"id":"INV-EFFECT-001","statement":"Unknown external effect never permits blind unsafe retry","enforcement":["tests/test_action_runtime.py","tests/test_supervision.py"]},
    {"id":"INV-DONE-001","statement":"Task completion requires an IMMUNE-owned verified completion manifest","enforcement":["tests/test_task_runtime.py","tests/test_immune.py"]},
    {"id":"INV-TRUTH-001","statement":"Production source cannot create new direct Evidence, completion-manifest, ledger-write, or legacy callable readback truth paths outside canonical owners","enforcement":["tests/test_truth_boundaries.py","tests/test_governed_evidence_boundary.py","tests/test_live_readback.py"]},
    {"id":"INV-STATE-001","statement":"Verified terminal completion closes Task, Step, and FEET route consistently","enforcement":["tests/test_kernel.py"]},
    {"id":"INV-VOICE-001","statement":"VOICE never upgrades tool/effect success into verified completion","enforcement":["tests/test_voice.py","tests/test_kernel.py"]},
    {"id":"INV-MEMORY-001","statement":"Retrieved memory never silently becomes current truth","enforcement":["tests/test_memory.py","tests/test_kernel.py"]},
    {"id":"INV-RECOVERY-001","statement":"Recovery reconstructs next work without replaying external commands","enforcement":["tests/test_supervision.py","tests/test_kernel.py"]},
    {"id":"INV-READINESS-001","statement":"Runtime readiness percentages are derived from canonical evidence-bearing state and 100 percent requires consistent VERIFIED completion","enforcement":["tests/test_readiness.py","tests/test_kernel.py"]},
    {"id":"INV-READINESS-002","statement":"BRO product readiness is computed from declared repository evidence criteria and tests derive expected scores from current criteria instead of stale hard-coded percentages","enforcement":["tests/test_product_readiness.py"]},
    {"id":"INV-DELIVERY-001","statement":"Changes reaching main must be associated with a merged pull request; direct main delivery fails closed","enforcement":["tests/test_main_delivery.py"]},
    {"id":"INV-DELIVERY-002","statement":"Protected architecture changes are delivered as declared subsystem remediation waves; micro-fixes require an explicit emergency reason","enforcement":["tests/test_remediation_wave.py"]},
    {"id":"INV-PLAN-001","statement":"Multi-step plans preserve canonical Step dependencies and only dependency-satisfied Steps become READY","enforcement":["tests/test_multistep.py"]},
    {"id":"INV-PLAN-002","statement":"A multi-step business outcome advances dependency-ready Steps on one canonical Task and only completes after all required evidence is VERIFIED","enforcement":["tests/test_multistep_execution.py"]},
    {"id":"INV-REPLAN-001","statement":"Only CURRENT CONFIRMED observations may automatically supersede unfinished Steps and create a new MIND Plan revision","enforcement":["tests/test_replan.py"]},
    {"id":"INV-ARTIFACT-001","statement":"HANDS-owned Artifact runtime revisions are immutable and VERIFIED requires sufficient IMMUNE Evidence from the same task boundary","enforcement":["tests/test_artifact_runtime.py"]}
  ]
}
